---
description: README の起動・ビルド手順を実態と同期させる
globs: README.md
alwaysApply: false
---

# README 整備（個人開発）

- **単一の入口**: クローン後に迷わないよう、依存インストール・起動・テスト・ビルドをこのファイルに集約する。
- **同期**: コマンドやポート・環境変数を変えたら、同じ変更で README を更新する。
- **例**: 必須の `env` は名前と用途だけでもよいので列挙する（値の秘密はプレースホルダでよい）。
